name: Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java (Android SDK)
        run: |
          sudo apt update -y
          sudo apt install -y openjdk-11-jdk
          sudo apt install -y unzip wget

      - name: Install Android SDK Build Tools
        run: |
          # Télécharger et extraire Android SDK Command-line tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-6609375_latest.zip -O commandlinetools.zip
          mkdir -p $HOME/android-sdk/cmdline-tools
          unzip commandlinetools.zip -d $HOME/android-sdk/cmdline-tools
          
          # Mettre à jour SDK et installer les outils nécessaires
          yes | $HOME/android-sdk/cmdline-tools/bin/sdkmanager --update
          yes | $HOME/android-sdk/cmdline-tools/bin/sdkmanager "platform-tools" "build-tools;30.0.3" "platforms;android-30"
          
          echo "$HOME/android-sdk" >> $GITHUB_PATH

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: Install dependencies
        run: |
          sudo apt install -y libncurses5 libstdc++6 libtinfo6

      - name: Build APK
        uses: digreatbrian/buildozer-action@v2
        with:
          python-version: 3.11
          buildozer-cmd: buildozer -v android debug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: package
          path: ./bin/*.apk
